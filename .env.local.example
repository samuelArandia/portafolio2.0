# Copy to .env.local and fill in the real value (never commit .env.local).
# URL of the Azure Logic Apps webhook that receives the contact form submission,
# including its SAS signature. Rotate this value if it was ever committed to git.
NEXT_PUBLIC_CONTACT_WEBHOOK_URL=
